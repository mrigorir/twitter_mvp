           
              <div class="d-flex mt-5">
                <%= image_tag 'squared_foto.jpg', class: 'img-fluid rounded', width: '80' %>
                <div class="d-flex flex-column flex-star ms-3">
                  <h6 class="text-dark fs-6 font-weight-bold">
                    <%= link_to @opinion.author.fullname, user_path(@opinion.author.id), class: 'text-decoration-none text-dark' %>
                  </h6>
                  <p class="c-feed fs-7">
                    <%= @opinion.text %>
                  </p>
                </div>
              </div>